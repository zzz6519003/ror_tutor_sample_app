#include "stdio.h"

int start[9];
int end[9];

int main() {
	int discRadius, pegs;
	scanf("%d %d", &discRadius, &pegs);
	for (int i = 1; i < discRadius; i++) {
		scanf("%d", start[i]);
	}
	for (int i = 1; i < discRadius; i++) {
		scanf("%d", end[i]);
	}

}

void init() {
	for (int i = 1; i < pegs; i++) {
		pegs[i] = 
	}
}